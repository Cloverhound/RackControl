<%= form_with(model: lab, :url => lab_rack_labs_path, local: true) do |form| %>
  <% if lab.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lab.errors.count, "error") %> prohibited this lab from being saved:</h2>

      <ul>
      <% lab.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>

  <div class="field" style="visibility:hidden">
    <%= form.label :lab_rack_id %>
    <%= form.text_field :lab_rack_id, value: params[:lab_rack_id] %>
  </div>
<% end %>
